#Task 1.1
#Display all data from the Customers table

SELECT *
FROM

#Task 1.2
#Retrieve only the first name (FirstName) and last name (LastName) of all customers from the Customers table.

SELECT FirstName, LastName
FROM Customers;
.
#Task 1.3
#Display the product name (ProductName) and price (Price) of all products from the Products table.

SELECT ProductName, Price
FROM Products;
